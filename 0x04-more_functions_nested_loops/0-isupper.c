#include "main.h"

/**
 * _isupper - function that checks for uppercase character
 * Return: 1 on success
 */

int _isupper(int c)
{
	if (c = 'A'; c <= 'Z'; c++)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
