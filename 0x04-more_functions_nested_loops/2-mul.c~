#include "main.h"

/**
 * mul - function that multiplies two intergers
 * @a, @b: the intergers that will be used as arguments in the function
 * 
 * Return: Always 0
 */

int mul(int a, int b)
{
	return (a * b);
}
