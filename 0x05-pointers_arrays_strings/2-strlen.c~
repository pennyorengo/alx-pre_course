#include "main.h"
#include <stdio.h>

/**
 * _strlen - returns the length of a string
 * @s: value of the character
 *
 * Return: Always 0

int _strlen(char *s)
{
	char *s;
	
	int len;

	*s = "My first Strlen!"
	
	len = _strlen(*s);
	
	
